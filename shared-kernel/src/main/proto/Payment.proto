syntax = "proto3";

package com.metao.book;

option java_multiple_files = true;
option java_package = "com.metao.book.shared";

enum Status {
  PENDING = 0;
  SUCCESSFUL = 1;
  FAILED = 2;
};

message UUID {
  // typedef string
}