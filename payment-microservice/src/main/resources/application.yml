management:
  otlp:
    metrics:
      export:
        enabled: false
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  tracing:
    export:
      enabled: true
      otlp:
        enabled: true
    sampling:
      probability: ${TRACING_SAMPLING_PROBABILITY:1.0}
  observations:
    annotations:
      enabled: true
  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: ${OTLP_TRACING_ENDPOINT:http://localhost:4318/v1/traces}
          transport: http
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
      show-components: always
      group:
        liveness:
          include: livenessState,ping
        readiness:
          include: readinessState,db,diskSpace

spring:
  threads:
    virtual:
      enabled: ${VIRTUAL_THREADS_ENABLED:false}
  web:
    error:
      include-message: always
      include-binding-errors: always
  application:
    name: payment-microservice
  kafka:
    bootstrap-servers: ${BOOTSTRAP_SERVERS}
    listener:
      ack-mode: manual_immediate
      observation-enabled: true
    template:
      observation-enabled: true
    properties:
      schema.registry.url: ${SCHEMA_REGISTRY_URL}
      # Topic subject order-payment-value already exists as AVRO in registry.
      # Use message-based subject to allow PROTOBUF without cross-type conflict.
      value.subject.name.strategy: io.confluent.kafka.serializers.subject.RecordNameStrategy
      basic.auth.credentials.source: USER_INFO
      basic.auth.user.info: ${SCHEMA_REGISTRY_AUTH}
      security.protocol: ${SECURITY_PROTOCOL}
      #sasl.mechanism: ${SASL_MECHANISM}
      ssl.truststore.type: ${SASL_TRUSTSTORE_TYPE}
      ssl.endpoint.identification.algorithm: "https"
      ssl.truststore.location: ${SASL_TRUSTSTORE_LOCATION}
      #ssl.truststore.password: ${TRUSTSTORE_PASSWORD}
      ssl.keystore.type: ${SASL_KEYSTORE_TYPE}
      ssl.keystore.location: ${SASL_KEYSTORE_LOCATION}
      #ssl.keystore.password: ${KEYSTORE_PASSWORD}
      #sasl.jaas.config: >
      #  org.apache.kafka.common.security.scram.ScramLoginModule required username="${SASL_USERNAME}" password="${SASL_PASSWORD}";
    consumer:
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      isolation-level: read_committed
      auto-offset-reset: none
      enable-auto-commit: false
      properties:
        session.timeout.ms: 30000
        max.poll.interval.ms: 300
        max.idle.time.ms: 60000
        heartbeat.interval.ms: 1000
        auto.offset.reset: earliest
        allow.auto.create.topics: "true"
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: io.confluent.kafka.serializers.protobuf.KafkaProtobufDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: io.confluent.kafka.serializers.protobuf.KafkaProtobufSerializer
      properties:
        enable.idempotence: true
  output.ansi.enabled: ALWAYS
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    hikari:
      auto-commit: false
      schema: "bookstore"
      maximum-pool-size: 10
      minimum-idle: 2
      max-lifetime: 600000      # 10 minutes
      idle-timeout: 300000      # 5 minutes
      keepalive-time: 300000    # ping connection
  flyway:
    enabled: true
    baseline-version: 0
    outOfOrder: false
    locations: classpath:migration
    clean-disabled: false
    create-schemas: true
    baseline-on-migrate: true
    validate-on-migrate: true
    default-schema: bookstore
  jpa:
    database: POSTGRESQL
    generate-ddl: false
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: validate
        jdbc:
          batch_size: 20
          order_inserts: true
          order_updates: true
          batch_versioned_data: true
        cache:
          use_query_cache: false
          use_second_level_cache: false
          region.factory_class: org.hibernate.cache.jcache.JCacheRegionFactory
          jcache:
            provider: com.github.benmanes.caffeine.jcache.spi.CaffeineCachingProvider
            configuration_resource_name: ehcache.xml
        connection:
          provider_disables_autocommit: true
        temp:
          use_jdbc_metadata_defaults: false
kafka:
  enabled: ${KAFKA_ENABLED:true}
  topic:
    order-created:
      id: order-created-id
      name: order-created
      group-id: order-created-group
      class-path: com.metao.book.shared.OrderCreatedEvent
    order-payment:
      id: order-payment-id
      name: order-payment
      group-id: order-payment-group
      class-path: com.metao.book.shared.OrderPaymentUpdatedEvent

logging:
  pattern.console: "%clr(%d{HH:mm:ss.SSS}){blue} %clr(---){faint} %clr([%t]){yellow} %clr([trace=%X{traceId:-},span=%X{spanId:-}]){magenta} %clr(:){red} %clr(%m){faint}%n"
